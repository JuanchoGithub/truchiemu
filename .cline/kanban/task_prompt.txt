Fix pre-existing build error in CoreDownloadSheet.swift line 268. The error on main (commit b8454f2) is an opaque return type issue with SwiftUI `some View`. On main, xcodebuild fails on CoreDownloadSheet.swift with an error about opaque return type or view builder issues. The section starts around line 268 with `private var coreDetailsCard: some View`. Investigate: check if there is a syntax error, missing view builder context, tuple view issue, or opaque type mismatch. Fix it so the project builds cleanly. Also clean up Xcode project references to 2 missing files: BoxArtDiscoveryService.swift and LaunchBoxAdvancedFilters.swift. Verify build succeeds after fixes.